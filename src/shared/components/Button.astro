---
/**
 * Button.astro
 * Responsabilidad: Componente atómico reutilizable de botón. Soporta
 * variantes visuales (primary, secondary, outline, ghost), tamaños y
 * estados (loading, disabled). Base de todos los botones del sistema.
 *
 * Tipos de datos: Props: { variant?: ButtonVariant, size?: ButtonSize,
 * disabled?: boolean, loading?: boolean, type?: HTMLButtonType }.
 */
---