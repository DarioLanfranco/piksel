---
/**
 * EmptyState.astro
 * Responsabilidad: Componente de estado vacío para mostrar cuando no hay
 * resultados en búsquedas, filtros o cuando el carrito está vacío. Incluye
 * icono, título, descripción y acción opcional.
 *
 * Tipos de datos: Props: { icon?: string, title: string, description?: string,
 * actionLabel?: string, onAction?: () => void }.
 */
---