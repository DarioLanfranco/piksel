---
/**
 * Skeleton.astro
 * Responsabilidad: Componente de placeholder / skeleton loader para mostrar
 * mientras se cargan datos. Ayuda a mejorar la percepción de rendimiento
 * y evita layout shifts.
 *
 * Tipos de datos: Props: { width?: string, height?: string, borderRadius?: string,
 * variant?: 'text' | 'circular' | 'rectangular', count?: number }.
 */
---