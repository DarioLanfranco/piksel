---
/**
 * Input.astro
 * Responsabilidad: Componente atómico de campo de entrada. Soporta tipos
 * (text, email, tel, number), estados de validación, mensajes de error,
 * y etiqueta flotante. Base para todos los formularios del sistema.
 *
 * Tipos de datos: Props: { label?: string, name: string, type?: string,
 * value?: string, error?: string, placeholder?: string, required?: boolean }.
 */
---