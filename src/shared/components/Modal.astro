---
/**
 * Modal.astro
 * Responsabilidad: Componente atómico de modal/diálogo overlayed. Soporta
 * tamaño, comportamiento de cierre (click outside, escape key), animación
 * de entrada/salida y slots para header, body y footer.
 *
 * Tipos de datos: Props: { isOpen: boolean, onClose: () => void,
 * size?: 'sm' | 'md' | 'lg', title?: string, closeOnOverlay?: boolean }.
 */
---