/**
 * validators.ts
 * Responsabilidad: Utilidades de validación de datos del lado del cliente.
 * Incluye validación de CUIT/CUIL (con dígito verificador), DNI, email,
 * teléfono argentino, y campos requeridos genéricos.
 *
 * Tipos de datos: funciones isValidCUIT(value): boolean, isValidEmail(value): boolean,
 * isValidPhone(value): boolean, isRequired(value): boolean, validateForm(data,
 * rules): ValidationResult con errores por campo.
 */