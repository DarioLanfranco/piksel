/**
 * common.types.ts
 * Responsabilidad: Tipos comunes y transversales compartidos por toda la
 * aplicación. Incluye genéricos de respuesta, tipos de estado (loading,
 * error, success), utilidades de unión, y tipos de UI compartidos.
 *
 * Tipos de datos: type AsyncState<T>, type Status, type Nullable<T>,
 * type ValidationResult, interface Option<T>, type DeepPartial<T>,
 * type Brand<K, T> para branded types.
 */