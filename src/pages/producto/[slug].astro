---
/**
 * [slug].astro
 * Responsabilidad: Página de detalle de producto generada dinámicamente.
 * Indexable por SEO con slugs amigables. Muestra la galería, especificaciones,
 * visor 3D, selector de accesorios, y botones de acción.
 *
 * Tipos de datos: params: { slug: string }. Usa ProductService para obtener
 * el producto por slug en SSR. Renderiza ProductGallery, ProductSpecs, Viewer3D.
 */
---
