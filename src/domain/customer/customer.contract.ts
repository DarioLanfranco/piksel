/**
 * customer.contract.ts
 * Responsabilidad: Puerto que define las operaciones de persistencia y
 * recuperación del perfil del cliente. La infraestructura implementa este
 * contrato (generalmente con localStorage o sessionStorage).
 *
 * Tipos de datos: métodos save(customer), get(), clear(), observable de
 * cambios. Retorna CustomerEntity o null.
 */