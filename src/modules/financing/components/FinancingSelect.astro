---
/**
 * FinancingSelect.astro
 * Responsabilidad: Componente de selección de cuotas de crédito. Muestra las
 * opciones disponibles (3, 6, 9, 12 cuotas) con el detalle del monto por
 * cuota y el precio final financiado.
 *
 * Tipos de datos: Props: { total: number, selected?: number, options?: Cuota[] }.
 * Emite: onSelect(cuotas: number). Calcula montos desde FinancingService.
 */
---