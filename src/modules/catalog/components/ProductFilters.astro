---
/**
 * ProductFilters.astro
 * Responsabilidad: Componente de filtros del catálogo. Permite filtrar por
 * marca, precio, capacidad y otras características. Emite eventos de cambio
 * que son consumidos por el store de catálogo.
 *
 * Tipos de datos: Props: { brands: string[], priceRange: [number, number],
 * capacities: string[] }. Emite: onFilterChange(filters: CatalogFilters).
 */
---