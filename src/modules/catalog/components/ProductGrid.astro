---
/**
 * ProductGrid.astro
 * Responsabilidad: Componente de presentación que renderiza la grilla principal
 * de productos del catálogo. Recibe una lista de productos y los muestra en un
 * layout responsivo de tarjetas. Puede manejar estados vacío, carga y error.
 *
 * Tipos de datos: Props: { products: ProductEntity[], loading?: boolean,
 * error?: string }. Renderiza múltiples ProductCard.
 */
---