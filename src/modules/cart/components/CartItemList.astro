---
/**
 * CartItemList.astro
 * Responsabilidad: Lista de items individuales dentro del carrito. Cada item
 * muestra imagen, nombre, cantidad seleccionada, precio y accesorios
 * elegidos. Permite modificar cantidad o eliminar el item.
 *
 * Tipos de datos: Props: { items: CartItem[] }.
 * Emite: onUpdateQuantity(productId, quantity), onRemove(productId).
 * CartItem incluye accesorios seleccionados.
 */
---