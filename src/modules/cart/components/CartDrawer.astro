---
/**
 * CartDrawer.astro
 * Responsabilidad: Panel deslizante lateral que muestra el contenido completo
 * del carrito. Incluye la lista de items, subtotal, botones de acción y
 * enlace al checkout. Se abre/cierra mediante estado global.
 *
 * Tipos de datos: Props: { isOpen?: boolean }. Lee del cart.store.
 * Renderiza CartItemList y CheckoutSummary parcial.
 */
---