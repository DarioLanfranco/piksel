---
/**
 * CartToggle.astro
 * Responsabilidad: Botón flotante o en la cabecera que muestra la cantidad
 * de items en el carrito y abre/cierra el drawer del carrito. Refleja el
 * estado del store en tiempo real.
 *
 * Tipos de datos: Props: { itemCount?: number }. Lee del cart.store.
 * Emite: evento de toggle al CartDrawer.
 */
---