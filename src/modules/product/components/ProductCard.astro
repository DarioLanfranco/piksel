---
/**
 * ProductCard.astro
 * Responsabilidad: Componente de presentación individual para cada producto
 * en la grilla del catálogo. Muestra imagen, nombre, precio, colores
 * disponibles y un botón de acción rápida.
 *
 * Tipos de datos: Props: { product: ProductEntity, layout?: 'grid' | 'list' }.
 * Renderiza enlaces a /productos/[slug].
 */
---