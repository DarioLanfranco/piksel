---
/**
 * TradeInForm.astro
 * Responsabilidad: Formulario para que el cliente especifique los datos del
 * dispositivo usado que entrega como parte de pago (Plan Canje). Incluye
 * marca, modelo, estado, capacidad y año aproximado.
 *
 * Tipos de datos: Props: { onSubmit?: (device: TradeInDevice) => void }.
 * Emite: TradeInDevice con todos los campos. Consume TradeInService.
 */
---