/**
 * site.config.ts
 * Responsabilidad: Configuración global del sitio (metatags, SEO base, Open Graph, etc.).
 * Esta capa pertenece al bootstrap de la aplicación y expone valores estáticos
 * que se consumen desde layouts y páginas.
 *
 * Tipos de datos: strings de metadatos (title, description, url, locale, etc.),
 * objetos de Open Graph, configuración de Twitter Cards.
 */